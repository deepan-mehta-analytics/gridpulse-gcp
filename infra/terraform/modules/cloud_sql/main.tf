# ── Cloud SQL module — skeleton, populated in Phase 3 ──────────────
# OLTP instance for the synthetic fleet/asset schema (SCD2 dimension
# source, replicated via Datastream CDC) lands here alongside the
# batch-backfill/gold-marts work. No resources declared yet — Phase 0
# bar is `terraform validate`/`plan` passing, not `apply`.
