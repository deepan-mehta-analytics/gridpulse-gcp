# ── Composer module — skeleton, populated in Phase 7 ───────────────
# Cloud Composer environment (local twin: Airflow via docker-compose)
# lands here only for the cloud demo window — Composer is not
# always-free, so it is stood up briefly and torn down rather than run
# continuously. No resources declared yet — Phase 0 bar is
# `terraform validate`/`plan` passing, not `apply`.
