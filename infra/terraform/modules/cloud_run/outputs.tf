# ── Cloud Run module outputs ───────────────────────────────────
output "module_status" { # honest status, not a placeholder comment buried in code
  description = "Populated once real resources land in this module"
  value       = "skeleton — not yet populated"
}
