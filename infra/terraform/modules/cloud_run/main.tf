# ── Cloud Run module — skeleton, populated in Phase 1 ──────────────
# Ingestion collector services and the FastAPI serving layer land here
# once ingest/collectors/ exists. No resources declared yet — Phase 0
# bar is `terraform validate`/`plan` passing, not `apply`.
