# ── Pub/Sub module — skeleton, populated in Phase 1 ────────────────
# Ingestion Cloud Run collectors publish to topics defined here once
# ingest/collectors/ lands. No resources declared yet — Phase 0 bar is
# `terraform validate`/`plan` passing, not `apply`.
