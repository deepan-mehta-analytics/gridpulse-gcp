# ── DLP module — skeleton, populated in Phase 4 ────────────────────
# PII profiling and policy-tag masking (ADR 0003, household smart-meter
# re-identification risk) land here once governance work begins. No
# resources declared yet — Phase 0 bar is `terraform validate`/`plan`
# passing, not `apply`.
