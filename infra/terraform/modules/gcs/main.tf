# ── GCS module — skeleton, populated in Phase 1 ────────────────────
# Raw landing bucket + lifecycle tiering rules for ingested payloads land
# here once the streaming ingest path is built. No resources declared
# yet — Phase 0 bar is `terraform validate`/`plan` passing, not `apply`.
