# ── BigLake module — skeleton, populated in Phase 3 ────────────────
# BigLake/Iceberg tables over the GCS lakehouse land here once batch
# backfill and gold-mart work begins. No resources declared yet —
# Phase 0 bar is `terraform validate`/`plan` passing, not `apply`.
