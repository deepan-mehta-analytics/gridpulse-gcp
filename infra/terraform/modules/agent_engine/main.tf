# ── Agent Engine module — skeleton, populated in Phase 6 ───────────
# Tier 2/3 agent deployment (schemas designed in Phase 0, built once the
# agent layer and eval harness land) lands here. No resources declared
# yet — Phase 0 bar is `terraform validate`/`plan` passing, not `apply`.
