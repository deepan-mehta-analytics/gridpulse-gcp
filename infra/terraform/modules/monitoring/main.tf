# ── Monitoring module — skeleton, populated in Phase 7 ─────────────
# Alert policies and INFORMATION_SCHEMA-backed cost dashboards land
# here alongside the reservations study during the cloud demo window.
# No resources declared yet — Phase 0 bar is `terraform validate`/
# `plan` passing, not `apply`.
