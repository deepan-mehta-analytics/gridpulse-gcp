# ── Dataplex module — skeleton, populated in Phase 4 ───────────────
# Zones, catalog entries, and DQ scans over silver/gold land here once
# governance work begins. Cloud-only, no local emulator twin —
# evidenced by Terraform plus a recorded walkthrough only (see repo
# CLAUDE.md). No resources declared yet — Phase 0 bar is
# `terraform validate`/`plan` passing, not `apply`.
