# ── Bigtable module — skeleton, populated in Phase 3 ───────────────
# Wide-column, hot-lookup comparison instance (ADR 0002, Bigtable vs
# BigQuery) lands here alongside the storage-layer work. No resources
# declared yet — Phase 0 bar is `terraform validate`/`plan` passing,
# not `apply`.
