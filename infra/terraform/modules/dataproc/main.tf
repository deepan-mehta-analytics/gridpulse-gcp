# ── Dataproc module — skeleton, populated in Phase 3 ───────────────
# Dataproc Serverless batch backfill (local twin: Spark in Docker)
# lands here once batch backfill and gold-marts work begins. No
# resources declared yet — Phase 0 bar is `terraform validate`/`plan`
# passing, not `apply`.
