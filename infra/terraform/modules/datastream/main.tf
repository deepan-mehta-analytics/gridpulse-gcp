# ── Datastream module — skeleton, populated in Phase 3 ─────────────
# CDC stream from Cloud SQL into bronze lands here alongside the
# batch-backfill/gold-marts work. Cloud-only, no local emulator twin —
# evidenced by Terraform plus a recorded walkthrough only (see repo
# CLAUDE.md). No resources declared yet — Phase 0 bar is
# `terraform validate`/`plan` passing, not `apply`.
