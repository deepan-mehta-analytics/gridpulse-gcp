# ── BigQuery module — skeleton, populated in Phase 1 ───────────────
# Bronze/silver/gold dataset scaffolding lands here once the streaming
# ingest path writes its first bronze rows. No resources declared yet —
# Phase 0 bar is `terraform validate`/`plan` passing, not `apply`.
